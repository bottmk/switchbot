/**
 * Parses `env.DEVICES` (a JSON string) into an array of `{deviceId, room}`.
 * @param {Record<string, string>} env
 * @returns {Array<{deviceId:string, room:string}>}
 */
export function loadDevices(env) {
  // TODO: parse env.DEVICES JSON
}
