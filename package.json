{
  "name": "switchbot-temperature-logger",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test 'test/**/*.test.js'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
